@model Dziennik.Models.Przedmiot

@{
    ViewBag.Title = "Szczegóły przedmiotu";
}
<!DOCTYPE html>

<html>
<meta charset="UTF-8" content="width=device-width" />
<style>
    body {
        margin: 0;
        padding: 0;
        font-family: sans-serif;
        background-image: url(../../Images/background_classroom.jpg);
        background-repeat: no-repeat;
        background-size: cover;
        color: white;
    }

    a {
        color: white;
    }

    .box {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%);
        width: 400px;
        padding: 40px;
        background: rgba(0,0,0,.8);
        box-sizing: border-box;
        box-shadow: 0 15px 25px rgba(0,0,0,.5);
        border-radius: 10px;
    }

        .box h2 {
            margin: 0 0 30px;
            padding: 0;
            color: white;
            text-align: center;
        }

        .box .inputBox {
            position: relative;
            margin-bottom: 5px;
        }

        .box .inputBoxStudents {
            position: relative;
            margin-bottom: 30px;
            border: 1px solid white;
            padding-bottom: 5px;
        }

        .box .inputBox .label {
            left: 0;
            padding: 10px 10px;
            font-size: 16px;
            color: white;
            pointer-events: none;
        }

        .box .summary .backButton .button {
            background: transparent;
            border: none;
            outline: none;
            color: white;
            background: #03a9f4;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            float: right;
            font-size: larger;
            text-decoration: none;
            margin-bottom: 30px;
        }

        .box .inputBox .button {
            background: transparent;
            font-size: 12px;
            border: none;
            outline: none;
            color: white;
            background: #03a9f4;
            padding: 5px 5px;
            cursor: pointer;
            border-radius: 5px;
            float: left;
            margin-right: 5px;
            text-decoration: none;
        }
</style>
</head>
<body style="background-image: url(../../Images/background_classroom.jpg);">
    <div class="box">
        <h2>@Html.DisplayFor(model => model.nazwa)</h2>
        <div class="inputBox">
            <a href="@Model.tresc_ksztalcenia" target="_blank" class="button">Treść kształcenia</a>
            <br /><br /><br />
            @Html.LabelFor(model => model.Nauczyciels, "Nauczyciele", htmlAttributes: new { @class = "label" })
        </div>
        <div class="inputBoxStudents">
            <ol>
                @foreach (var item in Model.Nauczyciels.OrderBy(u => u.Osoba.FullName))
                {
                    <li>@Html.DisplayFor(modelItem => item.Osoba.FullName)</li>
                }
            </ol>
        </div>
        <div class="inputBox">
            
        </div>
        <div class="summary">
            <span class="backButton">@Html.ActionLink("Cofnij", "Subjects", null, new { @class = "button" })</span>
        </div>
    </div>
</body>
</html>
